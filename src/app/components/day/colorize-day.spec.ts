import { ColorizeDay } from './colorize-day';

describe('ColorizeDay', () => {
  it('should create an instance', () => {
    const directive = new ColorizeDay();
    expect(directive).toBeTruthy();
  });
});
